import React from "react";
import {Text, TouchableOpacity, View} from "react-native";

/*onst ButtonToPlayers = ({onPress, title, onChangeText, value}) => (
    <View>
        <TouchableOpacity
            activeOpacity={0.8}
            onPress={onPress}
            style={<styles.anonymousButton}
        <
        <Text style={styles.anonymousButton}>{title}</Text>


</View>


)





const styles = StyleSheet.create({
    anonymousButton:{
*/

